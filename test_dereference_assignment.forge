def test_dereference_assignment() -> void {
    // Basic setup that we know works
    x: int = 42
    ptr: *int = &x

    // Your fix: dereference assignment
    *ptr = 100

    // Verify it worked by reading back
    result: int = *ptr
}

test_dereference_assignment()